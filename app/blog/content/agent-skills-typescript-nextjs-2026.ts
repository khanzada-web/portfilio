export const title = 'Agent Skills for TypeScript & Next.js Developers: Install, Use, and Create Custom Skills (2026)'
export const excerpt = 'Practical guide to Agent Skills — the open standard that packages engineering workflows for AI coding agents. Install Matt Pocock skills, understand progressive disclosure, and build production TypeScript/Next.js skills that work across Claude Code, Cursor, and Codex.'
export const content = `<div class=\"intro-section\"><h2>Why Agent Skills Matter More Than Another Coding Agent</h2><p class=\"lead-paragraph\">In August 2026 the conversation on X and GitHub shifted from which coding agent is best to how do we give any agent reliable engineering discipline. Agent Skills — folders that contain a SKILL.md file plus optional scripts and references — are the portable answer.</p><div class=\"key-highlights\"><h3>What You Will Learn</h3><ul><li>What Agent Skills actually are and how progressive disclosure keeps context cheap</li><li>How to install Matt Pocock production TypeScript skills</li><li>The exact SKILL.md format required by the open standard</li><li>How to write a custom skill for Next.js Server Actions and Prisma</li><li>Security, versioning, and common mistakes</li></ul></div></div><div class=\"technical-section\"><h2>1. What an Agent Skill Is (and Is Not)</h2><p>An Agent Skill is a directory whose only required file is <code>SKILL.md</code>. That file starts with YAML frontmatter (name + description) and continues with Markdown instructions the agent follows when the skill is active.</p><p>The open specification lives at <a href=\"https://agentskills.io/specification\" rel=\"noopener noreferrer\" target=\"_blank\">agentskills.io</a>. Anthropic originated the format; it is now adopted by Claude Code, Cursor, Codex CLI, Gemini CLI, and other tools.</p><p>Key properties:</p><ul><li><strong>Progressive disclosure</strong> — only name and description load at session start (about 30–50 tokens each). Full instructions load when the task matches.</li><li><strong>Composable</strong> — multiple skills can activate in the same session.</li><li><strong>Portable</strong> — the same SKILL.md works across agents.</li><li><strong>Version-controllable</strong> — skills live in the repo and travel with the project.</li></ul></div><div class=\"onpage-section\"><h2>2. Install Matt Pocock Skills</h2><p>Matt Pocock's <code>mattpocock/skills</code> collection is the highest-signal set for TypeScript engineers.</p><h3>Quick install</h3><pre><code>npx skills@latest add mattpocock/skills</code></pre><p>Or inside Claude Code:</p><pre><code>claude plugins install mattpocock-skills</code></pre><p>Then run the one-time setup:</p><pre><code>/setup-matt-pocock-skills</code></pre><p>High-value skills for Next.js / TypeScript:</p><ul><li><code>/grill-me</code> or <code>/grill-with-docs</code> — adversarial questioning before code</li><li><code>/to-prd</code> then <code>/to-issues</code> — conversation to PRD to vertical-slice issues</li><li><code>/tdd</code> — red-green-refactor loop</li><li><code>/codebase-design</code> — deep modules and clear seams</li><li><code>/diagnose</code> — reproduce, minimise, instrument, fix</li></ul></div><div class=\"content-strategy-section\"><h2>3. Anatomy of a Valid SKILL.md</h2><p>Every skill must follow the open format:</p><pre><code>---&#10;name: nextjs-server-actions&#10;description: Enforce thin Server Actions, Zod validation, and a server-only DAL for Next.js App Router. Use when creating or editing Server Actions or mutations.&#10;---&#10;&#10;# Next.js Server Actions Skill&#10;&#10;## Rules&#10;1. Every Server Action file starts with use server.&#10;2. Keep the action thin: validate with Zod, check session, call a server-only DAL, return a DTO.&#10;3. Never import Prisma directly inside the action file.&#10;4. Always scope queries by the authenticated user id.</code></pre><p>Frontmatter rules: <code>name</code> is required (lowercase, hyphens), <code>description</code> is the primary trigger — write it so the agent knows when to load the skill.</p></div><div class=\"local-seo-section\"><h2>4. Create a Custom Skill for Your Stack</h2><p>Highest ROI skills encode patterns your team already fights about.</p><h3>Step 1 — Create the directory</h3><pre><code>mkdir -p .agents/skills/nextjs-prisma-dal</code></pre><h3>Step 2 — Write SKILL.md</h3><pre><code>---&#10;name: nextjs-prisma-dal&#10;description: Enforce the Data Access Layer pattern for Next.js + Prisma. Use when writing Server Actions, API routes, Prisma queries, or ownership checks.&#10;---&#10;&#10;# Next.js + Prisma Data Access Layer&#10;&#10;## Non-negotiable architecture&#10;- Prisma client lives only in lib/prisma.ts (singleton).&#10;- All database access goes through data/ modules marked server-only.&#10;- Server Actions stay thin: validate, auth, DAL, map to DTO.&#10;&#10;## Ownership&#10;Every query that returns user data must include the authenticated user id in the WHERE clause.&#10;&#10;## Example&#10;// data/projects.ts&#10;import "server-only"&#10;import { prisma } from "@/lib/prisma"&#10;&#10;export async function getProjectForUser(projectId: string, userId: string) {&#10;  return prisma.project.findFirst({&#10;    where: { id: projectId, ownerId: userId },&#10;    select: { id: true, name: true, updatedAt: true },&#10;  })&#10;}</code></pre><h3>Step 3 — Verify triggering</h3><p>Restart the agent and ask it to add a create-project Server Action. A well-written description should cause the skill to load automatically.</p></div><div class=\"analytics-section\"><h2>5. Production Best Practices</h2><ul><li>Keep skills small and focused — one clear workflow per skill.</li><li>Put always-on context in AGENTS.md / CLAUDE.md.</li><li>Version skills with the repo.</li><li>Test the description — under-triggering is more common than over-triggering.</li><li>Prefer progressive disclosure for long reference material.</li></ul><h3>Security considerations</h3><p>Skills can contain executable scripts. Treat third-party skill repositories like npm packages: review SKILL.md and any scripts before installing. Prefer known authors (Anthropic, Matt Pocock, Addy Osmani, your own team).</p></div><div class=\"performance-section\"><h2>6. Common Mistakes</h2><ul><li>Vague descriptions that never trigger.</li><li>Putting everything in one mega-skill.</li><li>Duplicating CLAUDE.md content inside skills.</li><li>Forgetting ownership checks in examples.</li><li>Installing both the plugin and the npx copy.</li><li>Never running the setup skill.</li></ul></div><div class=\"use-cases-section\"><h2>7. Real Use Cases on a Next.js Codebase</h2><ul><li><strong>New feature</strong> — grill → to-prd → to-issues → tdd with the nextjs-prisma-dal skill keeping mutations safe.</li><li><strong>Bug investigation</strong> — diagnosing skill forces reproduce → minimise → instrument before a fix.</li><li><strong>Architecture review</strong> — improve-codebase-architecture report, then grill the deepenings.</li><li><strong>Onboarding</strong> — new teammates inherit Server Action and Prisma conventions from the shared skill set.</li></ul></div><div class=\"faq-section\"><h2>8. FAQ</h2><div class=\"faq-item\"><h3>Do Agent Skills work only with Claude?</h3><p>No. The format is an open standard. The same SKILL.md works with Claude Code, Cursor, Codex CLI, Gemini CLI, and other tools that implement the specification.</p></div><div class=\"faq-item\"><h3>Where should I put skills — personal or project?</h3><p>Project skills under .agents/skills/ or .claude/skills/ travel with the codebase. Personal skills under ~/.claude/skills/ suit cross-project workflows.</p></div><div class=\"faq-item\"><h3>How is a skill different from CLAUDE.md?</h3><p>CLAUDE.md is always loaded and should stay short. Skills use progressive disclosure and load only when the task matches the description.</p></div><div class=\"faq-item\"><h3>What if the agent ignores my skill?</h3><p>Almost always a description problem. Expand the description with phrases users actually type, then restart the agent.</p></div></div><div class=\"conclusion-section\"><h2>9. Summary</h2><p>Agent Skills are the practical layer between raw model capability and reliable engineering output. Install a proven collection such as Matt Pocock's for TypeScript discipline, then write thin, well-described skills that encode your Next.js, Prisma, and auth patterns.</p><div class=\"final-takeaway\"><h3>Key Takeaway</h3><p><em>A good skill is a portable onboarding guide for the agent: short metadata that always loads, full instructions that load only when needed, and concrete rules that match how your team ships production TypeScript.</em></p></div><hr><div class=\"cta-section\"><h3>Need help wiring AI coding agents into a production Next.js stack?</h3><p>I help teams design secure Server Actions, Prisma data layers, and agent-friendly project conventions. <a href=\"/contact\" style=\"color: #39FF14;\">Get in touch</a> or explore <a href=\"/services\" style=\"color: #39FF14;\">full-stack and AI-assisted development services</a>.</p><p>Related reading: <a href=\"/blog/meta-muse-code-typescript-nextjs-guide-2026\">Meta Muse Code for TypeScript & Next.js</a> and <a href=\"/blog/secure-server-actions-nextjs-16-auth-validation-dal\">Secure Server Actions in Next.js 16</a>.</p></div></div>`
export const date = '2026-08-12'
export const readTime = '12 min read'
export const category = 'AI & Full-Stack'
export const author = 'Mussawar Hayat'
export const keywords = [
  'Agent Skills',
  'Agent Skills TypeScript',
  'SKILL.md',
  'Matt Pocock skills',
  'AI coding agent skills',
  'Claude Code skills',
  'Cursor agent skills',
  'Next.js agent skills',
  'progressive disclosure agent skills',
  'how to create agent skill',
  'agentskills.io',
  'TypeScript AI agent workflows',
  'production Agent Skills 2026',
]
