export const title = 'Agent Skills for TypeScript & Next.js Developers: Install, Use, and Create Custom Skills (2026)'
export const excerpt = 'Practical guide to Agent Skills — the open standard that packages engineering workflows for AI coding agents. Install Matt Pocock skills, understand progressive disclosure, and build production TypeScript/Next.js skills that work across Claude Code, Cursor, and Codex.'
export const content = `<div class=\"intro-section\"><h2>Why Agent Skills Matter More Than Another Coding Agent</h2><p class=\"lead-paragraph\">In August 2026 the conversation on X and GitHub shifted from “which coding agent is best” to “how do we give any agent reliable engineering discipline.” Agent Skills — folders that contain a SKILL.md file plus optional scripts and references — are the portable answer. They turn a general-purpose model into a specialist that follows your TDD loop, your Prisma conventions, or your Next.js App Router patterns without you re-explaining them every session.</p><div class=\"key-highlights\"><h3>What You Will Learn</h3><ul><li>What Agent Skills actually are and how progressive disclosure keeps context cheap</li><li>How to install Matt Pocock’s production TypeScript skills in under a minute</li><li>The exact SKILL.md format and directory layout required by the open standard</li><li>How to write a custom skill for Next.js Server Actions, Prisma, or App Router patterns</li><li>Security, versioning, and common mistakes that break skill triggering</li><li>When skills beat long system prompts or CLAUDE.md-only setups</li></ul></div></div><div class=\"technical-section\"><h2>1. What an Agent Skill Is (and Is Not)</h2><p>An Agent Skill is a directory whose only required file is <code>SKILL.md</code>. That file starts with YAML frontmatter (name + description) and continues with Markdown instructions the agent follows when the skill is active. Optional subfolders hold scripts, reference docs, and assets.</p><p>The open specification lives at <a href=\"https://agentskills.io/specification\" rel=\"noopener noreferrer\" target=\"_blank\">agentskills.io</a>. Anthropic originated the format; it is now adopted by Claude Code, Cursor, Codex CLI, Gemini CLI, and a growing list of other tools. The same SKILL.md works across them.</p><p>Key properties:</p><ul><li><strong>Progressive disclosure</strong> — at session start the agent loads only the name and description of every installed skill (roughly 30–50 tokens each). Full instructions load only when the task matches the description.</li><li><strong>Composable</strong> — multiple skills can activate in the same session.</li><li><strong>Portable</strong> — no vendor lock-in. A skill written for Claude Code runs on Cursor or Codex with no rewrite.</li><li><strong>Version-controllable</strong> — skills live in the repo or a personal skills directory and travel with the project.</li></ul><p>A skill is not a prompt template you paste into chat. It is not a full agent harness. It is not a replacement for CLAUDE.md / AGENTS.md (those still hold always-on project context). Skills are on-demand workflows and domain expertise.</p></div><div class=\"onpage-section\"><h2>2. Install Matt Pocock Skills (TypeScript-First)</h2><p>Matt Pocock’s <code>mattpocock/skills</code> collection is the highest-signal set for TypeScript engineers. It packages real engineering loops — grilling requirements, vertical-slice issues, TDD, architecture deepening — instead of vibe-coding shortcuts.</p><h3>Quick install</h3><pre><code># Recommended for most agents (writes editable skill files into the project)\nnpx skills@latest add mattpocock/skills\n\n# Inside Claude Code you can also use the official plugin\nclaude plugins install mattpocock-skills\n# or from a session: /plugin install mattpocock-skills\n</code></pre><p>After install, run the one-time setup skill inside your agent:</p><pre><code>/setup-matt-pocock-skills\n</code></pre><p>It asks which issue tracker you use (GitHub, Linear, or local files), which triage labels you apply, and where generated docs should live. That config is consumed by <code>/to-issues</code>, <code>/triage</code>, and related skills.</p><h3>High-value skills for a Next.js / TypeScript codebase</h3><ul><li><code>/grill-me</code> or <code>/grill-with-docs</code> — adversarial questioning of a plan before any code is written</li><li><code>/to-prd</code> then <code>/to-issues</code> — conversation → structured PRD → vertical-slice GitHub issues</li><li><code>/tdd</code> — red-green-refactor loop enforced by the agent</li><li><code>/codebase-design</code> and <code>/improve-codebase-architecture</code> — deep modules, clear seams, dependency direction</li><li><code>/diagnose</code> / <code>/diagnosing-bugs</code> — disciplined reproduce → minimise → instrument → fix cycle</li></ul><p>These skills attack the four most common agent failure modes: misalignment on requirements, missing domain language, skipped tests, and architectural drift.</p></div><div class=\"content-strategy-section\"><h2>3. Anatomy of a Valid SKILL.md</h2><p>Every skill must follow the open format. Minimal example:</p><pre><code>---\nname: nextjs-server-actions\ndescription: Enforce thin Server Actions, Zod validation, and a server-only Data Access Layer for Next.js App Router. Use whenever the user creates or edits Server Actions, form handlers, or mutations in a Next.js project.\n---\n\n# Next.js Server Actions Skill\n\n## Rules\n1. Every Server Action file starts with \"use server\".\n2. Keep the action thin: validate input with Zod, check session, call a server-only DAL function, return a DTO.\n3. Never import Prisma or database clients directly inside the action file.\n4. Always scope queries by the authenticated user id (ownership check).\n5. Prefer returning { success, data } or { success: false, error } shapes.\n\n## Forbidden patterns\n- Returning full Prisma models to the client\n- Using \"use client\" in the same file as a Server Action\n- Skipping Zod validation because \"the form is trusted\"\n</code></pre><p>Frontmatter rules (from the specification):</p><ul><li><code>name</code> — required, 1–64 characters, lowercase letters, numbers, hyphens only</li><li><code>description</code> — required; this is the primary trigger. Write it so the agent knows <em>when</em> to load the skill, not only what it does. Be slightly “pushy” — include synonyms users actually type.</li><li>Optional: <code>license</code>, <code>metadata</code>, <code>compatibility</code></li></ul><p>Directory layout when you add scripts or references:</p><pre><code>nextjs-server-actions/\n├── SKILL.md\n├── scripts/          # optional executable helpers\n├── references/       # optional longer docs loaded on demand\n└── assets/           # optional templates\n</code></pre></div><div class=\"local-seo-section\"><h2>4. Create a Custom Skill for Your Stack</h2><p>The highest ROI skills encode the patterns your team already fights about. Below is a complete, ready-to-use skill for a typical Next.js 16 + Prisma + Auth.js SaaS.</p><h3>Step 1 — Create the directory</h3><pre><code># Project-scoped (shared with the team)\nmkdir -p .agents/skills/nextjs-prisma-dal\n# or personal\nmkdir -p ~/.claude/skills/nextjs-prisma-dal\n</code></pre><p>Many tools also discover skills under <code>.claude/skills/</code> and <code>.cursor/skills/</code>. Placing them in <code>.agents/skills/</code> maximises cross-tool reach.</p><h3>Step 2 — Write SKILL.md</h3><pre><code>---\nname: nextjs-prisma-dal\ndescription: Enforce the Data Access Layer pattern for Next.js + Prisma. Use when the user writes Server Actions, API routes, Prisma queries, or talks about data fetching, mutations, ownership checks, or connection pooling.\n---\n\n# Next.js + Prisma Data Access Layer\n\n## Non-negotiable architecture\n- Prisma client lives only in lib/prisma.ts (singleton).\n- All database access goes through data/ modules marked \"server-only\".\n- Server Actions and route handlers stay thin: validate → auth → DAL → map to DTO.\n\n## Ownership\nEvery query that returns user data must include the authenticated user id in the WHERE clause.\nNever trust a client-supplied id alone.\n\n## Connection health\nPrefer Prisma Accelerate or a proper pooler in production.\nNever create a new PrismaClient inside a Server Action or route handler.\n\n## Example shape\n\\\`\\\`\\\`ts\n// data/projects.ts\nimport \"server-only\"\nimport { prisma } from \"@/lib/prisma\"\n\nexport async function getProjectForUser(projectId: string, userId: string) {\n  return prisma.project.findFirst({\n    where: { id: projectId, ownerId: userId },\n    select: { id: true, name: true, updatedAt: true },\n  })\n}\n\\\`\\\`\\\`\n\n## When reviewing existing code\nFlag any file that imports @prisma/client outside lib/prisma.ts or data/.\nFlag any Server Action that returns a full Prisma model.\n</code></pre><h3>Step 3 — Verify triggering</h3><p>Restart the agent (or open a new session). Ask it to “add a create-project Server Action.” A well-written description should cause the skill to load automatically. If it does not, tighten the description with more trigger phrases the agent will actually see in user messages.</p></div><div class=\"analytics-section\"><h2>5. Production Best Practices</h2><ul><li><strong>Keep skills small and focused.</strong> One clear workflow per skill beats a 2 000-line mega-skill.</li><li><strong>Put always-on context in AGENTS.md / CLAUDE.md.</strong> Skills are for procedures that should load only when relevant.</li><li><strong>Version skills with the repo.</strong> When the team changes the DAL contract, update the skill in the same PR.</li><li><strong>Test the description.</strong> The description is the only thing that decides whether the skill activates. Under-triggering is more common than over-triggering.</li><li><strong>Prefer progressive disclosure.</strong> Put long reference material in <code>references/</code> so the agent loads it only when needed.</li><li><strong>Combine with Matt Pocock or Addy Osmani skills.</strong> Use their planning and TDD skills, then layer your stack-specific skills on top.</li></ul><h3>Security considerations</h3><p>Skills can contain executable scripts. Treat third-party skill repositories the same way you treat npm packages: review the SKILL.md and any scripts before installing, prefer pinned versions or lockfiles where available, and never install skills from untrusted sources into a production machine that holds secrets. Some community analyses have found a non-trivial percentage of marketplace skills with unsafe patterns; stick to known authors (Anthropic, Matt Pocock, Addy Osmani, your own team) until you have a review process.</p></div><div class=\"performance-section\"><h2>6. Common Mistakes</h2><ul><li><strong>Vague descriptions</strong> — “Helps with coding” never triggers. “Use when the user edits Server Actions, form handlers, or Prisma mutations in Next.js” does.</li><li><strong>Putting everything in one skill</strong> — context bloat and unclear activation.</li><li><strong>Duplicating CLAUDE.md content</strong> — skills should not restate the project tech stack; that belongs in the always-loaded project file.</li><li><strong>Forgetting ownership checks in examples</strong> — if the skill’s example code is insecure, the agent will copy the insecurity.</li><li><strong>Installing both the plugin and the npx copy</strong> — you end up with every skill twice and conflicting instructions.</li><li><strong>Never running the setup skill</strong> — Matt Pocock’s higher-level skills expect the tracker and label config that <code>/setup-matt-pocock-skills</code> writes.</li></ul></div><div class=\"use-cases-section\"><h2>7. Real Use Cases on a Next.js Codebase</h2><ul><li><strong>New feature</strong> — <code>/grill-with-docs</code> → <code>/to-prd</code> → <code>/to-issues</code> → <code>/tdd</code> while the nextjs-prisma-dal skill keeps every mutation safe.</li><li><strong>Bug investigation</strong> — invoke the diagnosing skill; the agent is forced through reproduce → minimise → instrument before proposing a fix.</li><li><strong>Architecture review</strong> — <code>/improve-codebase-architecture</code> produces a report; you grill the proposed deepenings before any large refactor.</li><li><strong>Onboarding</strong> — a new teammate installs the same skill set and immediately inherits the team’s Server Action and Prisma conventions without reading a 40-page wiki.</li></ul></div><div class=\"faq-section\"><h2>8. FAQ</h2><div class=\"faq-item\"><h3>Do Agent Skills work only with Claude?</h3><p>No. The format is an open standard. The same SKILL.md is discovered by Claude Code, Cursor, Codex CLI, Gemini CLI, and other tools that implement the specification.</p></div><div class=\"faq-item\"><h3>Where should I put skills — personal or project?</h3><p>Project skills (under <code>.agents/skills/</code> or <code>.claude/skills/</code> inside the repo) travel with the codebase and are ideal for team conventions. Personal skills (under <code>~/.claude/skills/</code> etc.) are better for your own cross-project workflows.</p></div><div class=\"faq-item\"><h3>How is a skill different from a system prompt or CLAUDE.md?</h3><p>CLAUDE.md / AGENTS.md is always loaded and should stay short. Skills load on demand via progressive disclosure, so you can keep a large library without paying the context cost on every turn.</p></div><div class=\"faq-item\"><h3>Can I ship skills inside an npm package?</h3><p>Yes. Some libraries already ship skills so that installing the package also installs the corresponding agent guidance. Check the package docs for the exact location the agent expects.</p></div><div class=\"faq-item\"><h3>What if the agent ignores my skill?</h3><p>Almost always a description problem. Expand the description with the exact phrases users (or you) type when the skill should fire. Restart the agent after changes.</p></div></div><div class=\"conclusion-section\"><h2>9. Summary</h2><p>Agent Skills are the practical layer that sits between raw model capability and reliable engineering output. Install a proven collection such as Matt Pocock’s for TypeScript discipline, then write thin, well-described skills that encode your Next.js, Prisma, and auth patterns. Keep descriptions trigger-focused, keep skills small, and version them with the code they protect.</p><div class=\"final-takeaway\"><h3>Key Takeaway</h3><p><em>A good skill is a portable onboarding guide for the agent: short metadata that always loads, full instructions that load only when needed, and concrete rules that match the way your team already ships production TypeScript.</em></p></div><hr><div class=\"cta-section\"><h3>Need help wiring AI coding agents into a production Next.js stack?</h3><p>I help teams design secure Server Actions, Prisma data layers, and agent-friendly project conventions. <a href=\"/contact\" style=\"color: #39FF14;\">Get in touch</a> or explore <a href=\"/services\" style=\"color: #39FF14;\">full-stack and AI-assisted development services</a>.</p><p>Related reading: <a href=\"/blog/meta-muse-code-typescript-nextjs-guide-2026\">Meta Muse Code for TypeScript &amp; Next.js</a> and <a href=\"/blog/secure-server-actions-nextjs-16-auth-validation-dal\">Secure Server Actions in Next.js 16</a>.</p></div></div>`
export const date = '2026-08-12'
export const readTime = '12 min read'
export const category = 'AI & Full-Stack'
export const author = 'Mussawar Hayat'
export const keywords = [
  'Agent Skills',
  'Agent Skills TypeScript',
  'SKILL.md',
  'Matt Pocock skills',
  'AI coding agent skills',
  'Claude Code skills',
  'Cursor agent skills',
  'Next.js agent skills',
  'progressive disclosure agent skills',
  'how to create agent skill',
  'agentskills.io',
  'TypeScript AI agent workflows',
  'production Agent Skills 2026',
]
