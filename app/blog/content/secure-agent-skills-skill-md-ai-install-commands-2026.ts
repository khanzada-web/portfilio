export const title = 'Secure Agent Skills in 2026: Treat SKILL.md as Executable Code'
export const excerpt = 'AI chat can hand you a poisoned download link. A restored backup can reinstall malware through a SKILL.md that looks like your own style guide. Here is a production security model for Agent Skills, install commands, and coding-agent config on TypeScript and Next.js teams.'
export const content = `<div class=\"intro-section\"><h2>Skills Are Code Now</h2><p class=\"lead-paragraph\">In late August 2026 a widely shared developer report made the same point twice. First, an AI chat recommended a download link for a transcription app. The site was a copycat. The install command looked normal. It was not. Second, after a clean wipe, a backup contained a SKILL.md that mimicked a personal writing-style skill — and instructed the agent to fetch the payload again whenever the skill loaded.</p><p>That combination is the new supply chain: models that cannot verify URLs, plus agent files that look like notes and behave like code. Anthropic’s own skills documentation already says malicious skills can introduce vulnerabilities, exfiltrate data, or drive unintended actions. The Agent Skills spec at <a href=\"https://agentskills.io/specification\" rel=\"noopener noreferrer\" target=\"_blank\">agentskills.io</a> explicitly allows a <code>scripts/</code> directory of executable code next to <code>SKILL.md</code>.</p><div class=\"key-highlights\"><h3>What You Will Learn</h3><ul><li>Why SKILL.md, hooks, CLAUDE.md, and AGENTS.md belong in the same threat model as shell scripts</li><li>How prompt injection, tool poisoning, and skill supply chain map to OWASP LLM and Agentic lists</li><li>A review checklist before you install any third-party skill</li><li>Safe patterns for AI-suggested install commands</li><li>Repo layout, git policy, and restore hygiene for Next.js / TypeScript teams</li></ul></div></div><div class=\"technical-section\"><h2>1. The Problem: Notes That Execute</h2><p>A skill is a folder with a required <code>SKILL.md</code> and optional <code>scripts/</code>, <code>references/</code>, and <code>assets/</code>. Compatible agents load name and description at session start. When a task matches, the full Markdown body is injected into context. If the body says run this script or download this file, a coding agent with shell and network tools will try to comply.</p><p>Three failure modes show up together:</p><ul><li><strong>Unverified install advice</strong> — confident curl or npm commands from a search-poisoned page.</li><li><strong>Skill / memory poisoning</strong> — a SKILL.md or style guide contains hidden instructions that run on every matching session.</li><li><strong>Restore replay</strong> — wiping the laptop is not enough if the poisoned file lives in a backup or dotfiles repo.</li></ul><p>OWASP’s LLM list still ranks prompt injection first (LLM01). The Agentic Applications list adds goal hijack, tool misuse, privilege abuse, and memory poisoning. Skills sit across those categories.</p></div><div class=\"onpage-section\"><h2>2. What Official Docs Already Warn</h2><ul><li>Anthropic: install skills only from trusted sources; audit files, scripts, and network instructions.</li><li>Claude Help Center: primary risks are prompt injection and data exfiltration; Enterprise can enable skill scanning.</li><li>Claude Code security docs: least privilege, sandboxed bash, working-directory boundary.</li><li>Agent Skills spec: scripts/ is first-class and meant to run.</li></ul></div><div class=\"content-strategy-section\"><h2>3. Review Checklist Before Install</h2><ul><li>Name the author and canonical repo without using the chat link.</li><li>Prefer pinned commits or tags.</li><li>Read every file: SKILL.md, hooks, scripts, package.json.</li><li>Search for curl, wget, fetch, .env access, wallet paths, SSH keys.</li><li>Flag “do not tell the user”, “run silently”, “skip confirmation”.</li><li>Prefer Markdown-only skills; reject opaque binaries.</li></ul></div><div class=\"local-seo-section\"><h2>4. Safe Pattern for AI-Suggested Install Commands</h2><p>The model may propose a command. You reconstruct it from a domain you typed yourself.</p><pre><code># Type the official domain yourself. Do not click the chat link.\nnpm view some-official-package dist.tarball\nnpm i some-official-package --ignore-scripts\n# Avoid: curl -fsSL https://unverified.example/install.sh | bash</code></pre><ul><li>Never pipe remote scripts from chat.</li><li>Never run sudo because an agent asked.</li><li>Pin versions.</li><li>First-run unknown CLIs in a throwaway VM.</li></ul></div><div class=\"analytics-section\"><h2>5. Production Layout for Next.js / TypeScript</h2><pre><code>your-nextjs-app/\n├── AGENTS.md\n├── .agents/skills/nextjs-prisma-dal/SKILL.md\n└── scripts/agent-skill-audit.sh</code></pre><p>Team skills go through PR. Skills must not read .env or phone home unless the host is named in the PR.</p><pre><code>---\nname: nextjs-prisma-dal\ndescription: Enforce thin Server Actions, Zod validation, and a server-only Prisma DAL. Use when adding or editing mutations. Do not use for package installs or credentials.\n---\n\n# Hard limits\n- Do not read environment files.\n- Do not run curl, wget, or remote installers.\n- Database access only through server-only DAL modules.</code></pre></div><div class=\"technical-section\"><h2>6. Git, Backups, and Restore Hygiene</h2><ul><li>Treat ~/.claude, ~/.codex, ~/.cursor, and .agents as executable config.</li><li>Diff before restore. Read every skill and MCP entry.</li><li>CI tripwire for banned patterns in skill folders.</li></ul><pre><code>#!/usr/bin/env bash\nset -euo pipefail\nroots=(\`.agents/skills\