export const title = 'Secure Agent Skills in 2026: Treat SKILL.md as Executable Code'
export const excerpt = 'AI chat can hand you a poisoned download link. A restored backup can reinstall malware through a SKILL.md. Production security model for Agent Skills and coding-agent config on TypeScript and Next.js teams.'
export const content = `<div class=\"intro-section\"><h2>Skills Are Code Now</h2><p class=\"lead-paragraph\">In late August 2026 a widely shared developer report made the same point twice. First, an AI chat recommended a download link for a transcription app. The site was a copycat. Second, after a clean wipe, a backup contained a SKILL.md that mimicked a personal writing-style skill and instructed the agent to fetch the payload again whenever the skill loaded.</p><p>Models cannot verify URLs. Agent files look like notes and behave like code. Anthropic warns that malicious skills can introduce vulnerabilities, exfiltrate data, or drive unintended actions. The Agent Skills spec at <a href=\"https://agentskills.io/specification\" rel=\"noopener noreferrer\" target=\"_blank\">agentskills.io</a> allows a <code>scripts/</code> directory next to <code>SKILL.md</code>.</p><div class=\"key-highlights\"><h3>What You Will Learn</h3><ul><li>Why SKILL.md belongs in the same threat model as shell scripts</li><li>Review checklist before third-party skill install</li><li>Safe handling of AI-suggested install commands</li><li>Repo layout, CI tripwires, and restore hygiene</li></ul></div></div><div class=\"technical-section\"><h2>1. The Problem: Notes That Execute</h2><p>A skill is a folder with <code>SKILL.md</code> and optional <code>scripts/</code>. Compatible agents inject the body into context when the description matches. Three failure modes: unverified install advice, skill or memory poisoning, and restore replay from backups or dotfiles.</p><p>Map this to OWASP LLM01 (prompt injection) and Agentic risks: goal hijack, tool misuse, privilege abuse, and memory poisoning.</p></div><div class=\"onpage-section\"><h2>2. Official Warnings</h2><ul><li>Anthropic: trusted sources only; audit scripts and network calls.</li><li>Claude Code: least privilege, sandbox, working-directory boundary.</li><li>agentskills.io: scripts are meant to run.</li></ul></div><div class=\"content-strategy-section\"><h2>3. Review Checklist</h2><ul><li>Verify author and canonical repo without chat links.</li><li>Pin commits or tags.</li><li>Read every file. Search for curl, wget, fetch, .env, SSH keys, silent execution.</li><li>Prefer Markdown-only skills. Reject opaque binaries.</li><li>Keep descriptions narrow so skills do not activate on every prompt.</li></ul></div><div class=\"local-seo-section\"><h2>4. AI Install Commands</h2><p>Never pipe remote scripts from chat. Reconstruct installs from official domains you type yourself. Prefer <code>npm i package --ignore-scripts</code> until you review files. Pin versions. First-run unknown CLIs in a throwaway VM.</p></div><div class=\"analytics-section\"><h2>5. Production Layout</h2><p>Keep team skills under <code>.agents/skills</code> in the repo so they go through pull request. Skills must not read <code>.env</code> or phone home unless the host is named in the PR. Auto mode is not allowed on machines with production secrets.</p></div><div class=\"technical-section\"><h2>6. Restore Hygiene</h2><p>Treat <code>~/.claude</code>, <code>~/.codex</code>, <code>~/.cursor</code>, and <code>.agents</code> as executable config. Diff before restore. Read every skill, hook, and MCP entry. After a bad install: disconnect secrets, rotate tokens, rebuild from known-good sources.</p></div><div class=\"performance-section\"><h2>7. Permissions and Agency</h2><ul><li>Supervised tool approval on production machines</li><li>Filesystem and network sandbox when available</li><li>Allowlist commands; least-privilege MCP tokens</li><li>Separate identities for experiments vs production</li></ul></div><div class=\"faq-section\"><h2>8. FAQ</h2><div class=\"faq-item\"><h3>Is Markdown really executable?</h3><p>The agent is the interpreter. With shell tools enabled, skill instructions become actions.</p></div><div class=\"faq-item\"><h3>Does sandboxing fix this?</h3><p>It shrinks blast radius. Review is still required.</p></div><div class=\"faq-item\"><h3>Repo or home directory?</h3><p>Team conventions in the repo via PR. Inspect personal skills after any incident.</p></div></div><div class=\"conclusion-section\"><h2>9. Summary</h2><p>If an agent will load it, treat it as code — SKILL.md included. Review third-party skills like dependencies. Reconstruct install commands from official domains you type yourself.</p><hr><div class=\"cta-section\"><h3>Building agent-safe Next.js systems?</h3><p><a href=\"/contact\" style=\"color: #39FF14;\">Get in touch</a> · <a href=\"/services\" style=\"color: #39FF14;\">Services</a></p><p>Related: <a href=\"/blog/agent-skills-typescript-nextjs-2026\">Agent Skills guide</a> · <a href=\"/blog/grok-bot-persistent-cloud-agents-production-guide-2026\">Grok Bot guardrails</a>.</p></div></div>`
export const date = '2026-09-05'
export const readTime = '10 min read'
export const category = 'AI & Full-Stack'
export const author = 'Mussawar Hayat'
export const keywords = [
  'secure agent skills',
  'SKILL.md security',
  'poisoned SKILL.md',
  'AI coding agent security',
  'Claude Code skills security',
  'agent skills supply chain',
  'TypeScript AI agent security 2026',
  'Next.js coding agent guardrails',
  'agentskills.io security',
]
