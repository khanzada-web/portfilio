export const title = 'Next.js August 2026 Security Release: AVIF Image RCE, Windows RCE, and a Production Patch Plan'
export const excerpt = 'Next.js 16.3.3 and 15.5.24 patch two critical unauthenticated RCE issues: AVIF image optimization via sharp/libheif, and a Windows filesystem Server Action path. Here is who is affected, what Vercel already covers, and the self-hosted checklist.'
export const content = `<div class=\"intro-section\"><h2>Two Critical RCEs, One Upgrade Window</h2><p class=\"lead-paragraph\">On 25 August 2026, the Next.js team shipped <strong>v16.3.3</strong> (Active LTS) and <strong>v15.5.24</strong> (Maintenance LTS) after moving the advertised security window forward by a day. The reason: a second critical issue landed in an upstream image dependency while the first Windows-only RCE was already queued.</p><p>This is not a nice-to-have minor. Both issues are unauthenticated remote code execution. If you self-host Next.js — Docker, VPS, Windows Node, a company VM — treat this as a same-week patch. If you host only on Vercel, official guidance is that managed Image Optimization already applied protections and no customer redeploy is required for these two advisories. Confirm that claim against the current Vercel changelog before you skip the upgrade anyway.</p><div class=\"key-highlights\"><h3>What You Will Learn</h3><ul><li>Official patched versions and the affected ranges</li><li>What the AVIF / sharp / libheif issue means for next/image</li><li>Who is exposed to CVE-2026-75604 on Windows</li><li>Why Cache Components is not a substitute for upgrading</li><li>A production checklist for Linux VPS, Docker, and mixed App + Pages apps</li></ul></div></div><div class=\"technical-section\"><h2>1. Official Versions</h2><p>Patched releases named by the Next.js security post:</p><ul><li><code>next@16.3.3</code> for the 16.x Active LTS line</li><li><code>next@15.5.24</code> for the 15.x Maintenance LTS line</li><li>Canary line: <code>16.4.0-canary.7</code> and later for teams tracking canary</li></ul><p>Publicly reported affected ranges include 13.4 through 15.5.23 and 16.0.x through 16.3.2, plus matching canaries. Pin the exact patched version in production. Do not stop at &quot;latest 16&quot; if your lockfile still resolves 16.3.2.</p><pre><code>npm install next@16.3.3\n# or\nnpm install next@15.5.24\n\nnpm ls next</code></pre><p>Rebuild the standalone output or container image after the bump. A running Node process that loaded the old <code>node_modules/next</code> is still the old binary.</p></div><div class=\"onpage-section\"><h2>2. Issue A — AVIF Image Optimization (Critical)</h2><p>Tracked as unauthenticated RCE in the Image Optimization API when AVIF is processed. The root cause is not application code. It is a vulnerability in <strong>libheif</strong>, the library used by <strong>sharp</strong>, which Next.js uses to resize images.</p><p>If an attacker can get Next.js to optimize a crafted AVIF — typically by requesting the image optimizer with a reachable source — the decoder path can execute code. The official patch does not wait for an upstream libheif fix. It <strong>disables AVIF optimization</strong> in the patched Next.js releases until that upstream fix lands.</p><p>Production implications:</p><ul><li>Self-hosted apps that still advertise AVIF in <code>images.formats</code> should drop <code>image/avif</code> until Next.js re-enables it.</li><li>Do not point <code>remotePatterns</code> at open hosts. An open remote image allow-list plus a public optimizer is how untrusted bytes reach sharp.</li><li>If you front Next.js with your own image CDN, confirm whether that CDN still decodes AVIF with a vulnerable libheif.</li><li>Vercel stated it disabled AVIF on the managed optimizer when the issue was identified. Self-hosted Nginx + standalone Node does not get that for free.</li></ul><p>WebP remains the safe default format for marketing sites. Users do not notice the missing AVIF if LCP images are already compressed WebP.</p></div><div class=\"content-strategy-section\"><h2>3. Issue B — Windows Filesystem RCE (CVE-2026-75604)</h2><p>The second critical issue is <strong>CVE-2026-75604</strong> (GHSA-p293-qw3h-jr36). Official impact statement: unauthenticated remote code execution on <strong>Windows-hosted</strong> Next.js servers when the app uses both the Pages Router and the App Router <strong>without Cache Components</strong>.</p><p>Linux and macOS are not affected by this second issue. There is no official workaround for Windows hosts other than upgrading.</p><p>Public analysis of the fix describes a path-separator gap: backslash is a directory delimiter on Windows and was not treated the same as forward slash during cache-path sanitization. That class of bug is enough to walk out of a cache directory and reach internal server files used in the Server Action trust path.</p><p>This guide does not include exploit steps. You do not need them to decide to patch. If your production Node process runs on Windows, assume exposure until 15.5.24 or 16.3.3 is running.</p><p>Cache Components being enabled is mentioned as a condition that changes the surface. It is <strong>not</strong> an approved mitigation. Enabling a caching flag is not a security program. Upgrade.</p></div><div class=\"local-seo-section\"><h2>4. Who Must Move This Week</h2><ul><li><strong>Vercel-only, no self-hosted preview:</strong> platform protections cover these two issues. Still align local and CI to 16.3.3 so preview builds match production.</li><li><strong>Linux VPS / Docker / Coolify / Railway / Render:</strong> patch Next.js, drop AVIF from <code>images.formats</code>, restrict <code>remotePatterns</code>, redeploy standalone.</li><li><strong>Windows Server / Azure Windows:</strong> emergency patch. No workaround.</li><li><strong>Mixed App Router + leftover Pages API:</strong> higher priority. The Windows advisory explicitly calls out dual-router apps.</li><li><strong>Internal admin Next.js behind SSO:</strong> still patch. Unauthenticated means the Next.js process itself is the trust boundary, not your login page.</li></ul></div><div class=\"analytics-section\"><h2>5. Production Checklist</h2><ol><li>Bump <code>next</code> and <code>eslint-config-next</code> together so lint plugins match the compiler.</li><li>Commit the lockfile. Confirm <code>npm ls next</code> prints only the patched version.</li><li>Set <code>images.formats</code> to <code>[\"image/webp\"]</code> until AVIF is officially re-enabled.</li><li>Tighten <code>images.remotePatterns</code> to first-party hosts.</li><li>Confirm <code>output: \"standalone\"</code> images copy the new <code>.next/standalone</code> tree, not an old layer cache.</li><li>On Windows hosts, schedule the deploy as a security change, not a feature train.</li><li>After deploy, hit a known <code>next/image</code> URL and confirm AVIF is no longer negotiated if you removed the format.</li><li>Re-run auth and Server Action smoke tests.</li></ol><p>Keep treating every Server Action as a public endpoint. The July 2026 release already covered Server Action and middleware hardening. This August release does not replace that work. See the earlier guide on <a href=\"/blog/nextjs-july-2026-security-patch-server-actions\">the July 2026 Server Actions patch</a> and <a href=\"/blog/secure-server-actions-nextjs-16-auth-validation-dal\">auth, validation, and DAL patterns</a>.</p></div><div class=\"technical-section\"><h2>6. Linux VPS + Nginx Notes</h2><p>Most production work I ship runs Next.js standalone behind Nginx on Linux. That stack is outside the Windows RCE. It is not outside the AVIF optimizer issue.</p><p>Typical gaps:</p><ul><li>A Docker layer cache that reuses <code>node_modules</code> from last week</li><li><code>images.formats</code> still listing AVIF because a Lighthouse article recommended it in 2024</li><li><code>remotePatterns</code> copied from a tutorial with a wildcard hostname</li></ul><p>Fix the image config in the same PR as the version bump. Then invalidate the Docker build cache for the deps stage. If you use multi-stage builds, the standalone copy step is where old binaries hide. Layout notes: <a href=\"/blog/docker-multi-stage-nextjs-16-standalone-production\">Docker multi-stage builds for Next.js 16</a>.</p></div><div class=\"faq-section\"><h2>FAQ</h2><div class=\"faq-item\"><h3>Does hosting on Vercel mean I can ignore 16.3.3?</h3><p>Vercel says applications on its platform are protected from these two August 2026 issues and do not require a customer redeploy. You should still upgrade local, preview, and any non-Vercel copy of the same repo so you are not developing against a vulnerable optimizer.</p></div><div class=\"faq-item\"><h3>Are Linux servers safe from CVE-2026-75604?</h3><p>The official advisory says Linux and macOS are not affected by the Windows filesystem RCE. They are still in scope for the AVIF optimizer issue if you self-host Image Optimization.</p></div><div class=\"faq-item\"><h3>Is enabling Cache Components enough?</h3><p>No. Cache Components changes part of the Windows surface. It is not an approved workaround. Upgrade to 16.3.3 or 15.5.24.</p></div><div class=\"faq-item\"><h3>Should I keep serving AVIF?</h3><p>Not from Next.js Image Optimization until the project re-enables it after an upstream libheif fix. Serve WebP. Revisit AVIF when the official release notes say the decoder path is safe again.</p></div><div class=\"faq-item\"><h3>I only use the App Router. Am I out of scope for the Windows issue?</h3><p>The official wording highlights apps that use both routers without Cache Components. If the process runs on Windows, patch. Do not parse the dual-router sentence as a reason to wait.</p></div></div><div class=\"conclusion-section\"><h2>Summary</h2><p>August 2026 is a two-bug release: a decoder RCE in the AVIF path, and a Windows path-sanitization RCE that can reach Server Action trust material. The correct response is a version pin, a lockfile commit, an image-format change, and a clean rebuild of standalone or container artifacts.</p><div class=\"final-takeaway\"><h3>Key Takeaway</h3><p><em>Patch to Next.js 16.3.3 or 15.5.24 this week. Turn off AVIF optimization. Do not use Cache Components or a Linux hostname as an excuse to skip the upgrade on every environment that still runs the old binary.</em></p></div><hr><div class=\"cta-section\"><h3>Need a self-hosted Next.js patch and rebuild?</h3><p>I upgrade production App Router apps on VPS and Docker, including standalone output, Nginx, and image pipeline changes. <a href=\"/contact\" style=\"color: #39FF14;\">Get in touch</a> or review <a href=\"/services\" style=\"color: #39FF14;\">full-stack and DevOps services</a>.</p><p>Related reading: <a href=\"/blog/nextjs-july-2026-security-patch-server-actions\">July 2026 Server Actions security patch</a>, <a href=\"/blog/secure-server-actions-nextjs-16-auth-validation-dal\">Secure Server Actions in Next.js 16</a>, and <a href=\"/blog/docker-multi-stage-nextjs-16-standalone-production\">Docker multi-stage production builds</a>.</p></div></div>`
export const date = '2026-08-30'
export const readTime = '11 min read'
export const category = 'Security'
export const author = 'Mussawar Hayat'
export const keywords = [
  'Next.js August 2026 security release',
  'Next.js 16.3.3',
  'Next.js 15.5.24',
  'CVE-2026-75604',
  'GHSA-p293-qw3h-jr36',
  'Next.js AVIF RCE',
  'sharp libheif Next.js',
  'Windows Next.js RCE',
  'next/image security',
  'self-hosted Next.js patch',
  'Cache Components security',
  'Next.js production upgrade',
]
