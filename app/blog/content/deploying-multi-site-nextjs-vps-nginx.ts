export const title = 'Multi-Site Next.js on VPS with Nginx'
export const excerpt = 'Running multiple Next.js apps on one VPS with Nginx reverse proxy, PM2, and SSL — the exact setup I use for production deployments.'
export const content = `<div class=\"intro-section\"><h2>One VPS, Many Next.js Apps</h2><p class=\"lead-paragraph\">A single VPS can host several Next.js sites with isolated Node processes, Nginx as the TLS terminator and reverse proxy, and PM2 for process management. This guide covers the layout I use in production: directory structure, Nginx server blocks, PM2 ecosystem files, and SSL renewal.</p><div class=\"key-highlights\"><h3>What You Will Learn</h3><ul><li>Directory and port allocation per app</li><li>Nginx reverse proxy and HTTP/2</li><li>PM2 ecosystem config for multiple apps</li><li>Certbot SSL and auto-renewal</li><li>Zero-downtime deploy script pattern</li></ul></div></div><div class=\"technical-section\"><h2>1. Layout</h2><pre><code>/var/www/\n  site-a/current -> releases/...\n  site-b/current -> releases/...\n/etc/nginx/sites-available/\n  site-a.conf\n  site-b.conf\n</code></pre><p>Each app listens on a unique localhost port (3001, 3002, ...). Nginx proxies public hostnames to those ports.</p></div><div class=\"onpage-section\"><h2>2. Nginx Server Block</h2><pre><code>server {\n  listen 443 ssl http2;\n  server_name example.com www.example.com;\n  ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;\n  ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;\n  location / {\n    proxy_pass http://127.0.0.1:3001;\n    proxy_http_version 1.1;\n    proxy_set_header Upgrade $http_upgrade;\n    proxy_set_header Connection \"upgrade\";\n    proxy_set_header Host $host;\n    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n    proxy_set_header X-Forwarded-Proto $scheme;\n  }\n}\n</code></pre></div><div class=\"content-strategy-section\"><h2>3. PM2 Ecosystem</h2><pre><code>module.exports = {\n  apps: [\n    { name: \"site-a\", cwd: \"/var/www/site-a/current\", script: \"node_modules/next/dist/bin/next\", args: \"start -p 3001\", env: { NODE_ENV: \"production\" } },\n    { name: \"site-b\", cwd: \"/var/www/site-b/current\", script: \"node_modules/next/dist/bin/next\", args: \"start -p 3002\", env: { NODE_ENV: \"production\" } },\n  ],\n}\n</code></pre></div><div class=\"local-seo-section\"><h2>4. Deploy Pattern</h2><p>Build on CI or on the server into a new release directory, flip the <code>current</code> symlink, then <code>pm2 reload</code> the app name. Keep the previous release for instant rollback.</p></div><div class=\"conclusion-section\"><h2>5. Production Checklist</h2><ul><li>Unique ports per app; never expose Node ports publicly</li><li>TLS via Certbot with renew hooks reloading Nginx</li><li>Firewall: only 80/443 (and SSH) open</li><li>Log rotation for Nginx and PM2</li><li>Health checks and disk alerts</li></ul><h2>Summary</h2><p>Nginx + PM2 + symlink releases is a durable, low-cost way to run multiple Next.js production sites on one VPS.</p><div class=\"final-takeaway\"><h3>Key Takeaway</h3><p><em>Isolate processes by port, terminate TLS at Nginx, and deploy with symlink flips plus PM2 reload.</em></p></div><hr><div class=\"cta-section\"><h3>Need a production VPS setup?</h3><p>I configure multi-site Next.js hosting, SSL, and deploy pipelines on VPS. <a href=\"/contact\" style=\"color: #39FF14;\">Get in touch</a>.</p></div></div>`
export const date = '2026-05-15'
export const readTime = '9 min read'
export const category = 'DevOps'
export const author = 'Mussawar Hayat'
export const keywords = ['Next.js VPS', 'Nginx', 'PM2', 'multi-site', 'SSL', 'deployment', 'reverse proxy']
