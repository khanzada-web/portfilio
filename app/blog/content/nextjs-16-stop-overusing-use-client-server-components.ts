export const title = "Stop Overusing 'use client' in Next.js 16"
export const excerpt = 'The most common App Router mistake is marking entire trees with use client. Learn the production patterns for Server Components, children slots, and minimal client boundaries that keep your bundles small and your data secure.'
export const content = `<div class="intro-section"><h2>The Most Common App Router Mistake</h2><p class="lead-paragraph">The most common App Router mistake is marking entire trees with <code>use client</code>. That forces the whole subtree into the client bundle, leaks server-only data patterns, and defeats Server Components. Production apps keep client boundaries small: interactive leaves only, with Server Components as the default for data and layout.</p><div class="key-highlights"><h3>What You Will Learn</h3><ul><li>When use client is required and when it is not</li><li>Children slots so Server Components stay server-side</li><li>Minimal client islands for forms, charts, and motion</li><li>How to audit an existing codebase for overuse</li><li>Bundle and security benefits of thin client boundaries</li></ul></div></div><div class="technical-section"><h2>1. Server Components Are the Default</h2><p>In the App Router, files without <code>use client</code> are Server Components. They can fetch data, access secrets, and render HTML without shipping their module graph to the browser. Adding <code>use client</code> at the top of a layout or page pulls that file and every import into the client bundle.</p></div><div class="onpage-section"><h2>2. When You Actually Need use client</h2><ul><li>Event handlers (onClick, onChange, onSubmit)</li><li>React hooks (useState, useEffect, useRef, custom hooks)</li><li>Browser-only APIs (window, localStorage, IntersectionObserver)</li><li>Third-party libraries that assume a browser environment</li></ul><p>If a component only displays data, it does not need <code>use client</code>.</p></div><div class="content-strategy-section"><h2>3. Children Slots Keep Parents Server-Side</h2><p>A Server Component can render a Client Component and pass Server Component children into it. The children stay on the server; only the client wrapper hydrates.</p><pre><code>// ClientShell.tsx\n'use client'\nexport function ClientShell({ children }: { children: React.ReactNode }) {\n  return &lt;div className="shell"&gt;{children}&lt;/div&gt;\n}\n\n// page.tsx (Server Component)\nimport { ClientShell } from './ClientShell'\nimport { HeavyServerList } from './HeavyServerList'\n\nexport default async function Page() {\n  return (\n    &lt;ClientShell&gt;\n      &lt;HeavyServerList /&gt;\n    &lt;/ClientShell&gt;\n  )\n}\n</code></pre></div><div class="local-seo-section"><h2>4. Client Islands, Not Client Pages</h2><p>Prefer small interactive leaves: a like button, a filter dropdown, a chart. Fetch and compose data in Server Components; pass serializable props into the island.</p></div><div class="linkbuilding-section"><h2>5. Audit Pattern</h2><ol><li>Search the repo for <code>'use client'</code></li><li>For each match, ask: does this file use hooks, events, or browser APIs?</li><li>If not, remove the directive and move interactive children into their own client files</li><li>Measure bundle size before and after</li></ol></div><div class="analytics-section"><h2>6. Security and Performance Wins</h2><ul><li>Server-only modules stay off the client graph</li><li>Smaller JS improves TTI and Core Web Vitals</li><li>Secrets and database clients cannot leak via client imports</li></ul></div><div class="conclusion-section"><h2>7. Production Checklist</h2><ul><li>Default every new file to Server Component</li><li>Add use client only at the interactive leaf</li><li>Use children slots for composition</li><li>Never import Prisma, fs, or auth helpers into client files</li></ul><h2>Summary</h2><p>Overusing use client is a Pages Router habit. Treat the client as an opt-in boundary for interaction, not the default for pages.</p><div class="final-takeaway"><h3>Key Takeaway</h3><p><em>Server Components by default, client islands at the leaves, children slots for composition.</em></p></div><hr><div class="cta-section"><h3>Want a Server Components audit?</h3><p>I review App Router trees for unnecessary client boundaries. <a href="/contact" style="color: #39FF14;">Get in touch</a>.</p></div></div>`
export const date = '2026-07-26'
export const readTime = '13 min read'
export const category = 'Full-Stack'
export const author = 'Mussawar Hayat'
export const keywords = ['Next.js 16', 'Server Components', 'use client', 'App Router', 'React Server Components', 'bundle size']
