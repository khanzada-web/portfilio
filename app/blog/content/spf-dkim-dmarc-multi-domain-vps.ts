export const title = 'SPF, DKIM & DMARC for Multi-Domain VPS'
export const excerpt = 'Email deliverability is silent until it breaks. Here is the exact DNS setup I use to keep transactional email out of spam across multiple domains.'
export const content = `<div class=\"intro-section\"><h2>Deliverability Is Infrastructure</h2><p class=\"lead-paragraph\">Transactional email from a VPS fails quietly when SPF, DKIM, and DMARC are missing or inconsistent. Multi-domain hosts need careful DNS and aligned From domains. This is the setup I use for production mail on shared VPS environments.</p><div class=\"key-highlights\"><h3>What You Will Learn</h3><ul><li>SPF records that authorize only your senders</li><li>DKIM key generation and DNS publishing</li><li>DMARC policies that start monitoring then enforce</li><li>Alignment pitfalls with multiple domains</li><li>Checklist before sending production mail</li></ul></div></div><div class=\"technical-section\"><h2>1. SPF</h2><p>Publish a TXT record that lists only the IPs or includes you actually use. Avoid <code>+all</code>. Example: <code>v=spf1 ip4:YOUR.IP -all</code> or include your ESP.</p></div><div class=\"onpage-section\"><h2>2. DKIM</h2><p>Generate a key pair on the mail host, configure the MTA to sign, and publish the public key as a TXT record under a selector (e.g. <code>mail._domainkey</code>). Rotate keys on a schedule.</p></div><div class=\"content-strategy-section\"><h2>3. DMARC</h2><p>Start with <code>v=DMARC1; p=none; rua=mailto:dmarc@yourdomain.com</code> to collect reports. Move to <code>quarantine</code> then <code>reject</code> once legitimate mail passes alignment.</p></div><div class=\"local-seo-section\"><h2>4. Multi-Domain Notes</h2><p>Each sending domain needs its own SPF/DKIM/DMARC. The visible From domain must align with DKIM/SPF for DMARC to pass. Shared IP reputation affects all domains on the box — monitor blacklists.</p></div><div class=\"conclusion-section\"><h2>5. Production Checklist</h2><ul><li>SPF hard fail (-all) with correct includes</li><li>DKIM signing verified with external checkers</li><li>DMARC rua mailbox monitored</li><li>Reverse DNS (PTR) matches HELO name</li><li>TLS for outbound submission</li></ul><h2>Summary</h2><p>SPF, DKIM, and DMARC are not optional for transactional mail. Align domains, start DMARC in monitor mode, then enforce.</p><div class=\"final-takeaway\"><h3>Key Takeaway</h3><p><em>Authorize narrowly with SPF, sign with DKIM, and enforce with DMARC after you have clean reports.</em></p></div><hr><div class=\"cta-section\"><h3>Email from your VPS landing in spam?</h3><p>I set up SPF, DKIM, DMARC, and mail stacks for multi-domain hosts. <a href=\"/contact\" style=\"color: #39FF14;\">Get in touch</a>.</p></div></div>`
export const date = '2026-04-28'
export const readTime = '7 min read'
export const category = 'DevOps'
export const author = 'Mussawar Hayat'
export const keywords = ['SPF', 'DKIM', 'DMARC', 'email deliverability', 'VPS', 'DNS', 'transactional email']
