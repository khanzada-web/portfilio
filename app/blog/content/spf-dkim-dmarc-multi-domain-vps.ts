export const title = 'SPF, DKIM & DMARC for Multi-Domain VPS: Production Email Setup (2026)'
export const excerpt = 'Exact DNS and mail-server setup for SPF, DKIM, and DMARC on a multi-domain VPS. Keep transactional email out of spam with alignment, monitoring, and a production checklist.'
export const content = `<div class=\"intro-section\"><h2>Deliverability Is Infrastructure, Not a Plugin</h2><p class=\"lead-paragraph\">Transactional email from a VPS fails quietly when SPF, DKIM, or DMARC are missing or misaligned. Password resets, invoices, and contact forms land in spam — or never arrive — while the application log still shows “sent.” Multi-domain hosts amplify the problem: each From domain needs its own authorization records, and a shared IP’s reputation affects every site on the box.</p><p>This guide documents the production setup used for multi-domain VPS mail: narrow SPF, working DKIM signatures, DMARC that starts in monitor mode and moves to enforce, plus reverse DNS and TLS requirements receivers actually check.</p><div class=\"key-highlights\"><h3>What You Will Learn</h3><ul><li>SPF records that authorize only your real senders</li><li>DKIM key generation, signing, and DNS publication</li><li>DMARC policies from p=none to p=reject with rua reporting</li><li>Alignment rules when several domains share one VPS</li><li>A pre-production checklist including PTR and TLS</li></ul></div></div><div class=\"technical-section\"><h2>1. SPF: Authorize Narrowly</h2><p>SPF is a DNS TXT record that lists which hosts may send mail for a domain. Publish one SPF record per domain (multiple SPF TXT records are invalid).</p><p>Examples:</p><ul><li>Mail only from this VPS: <code>v=spf1 ip4:203.0.113.10 -all</code></li><li>VPS plus Google Workspace: <code>v=spf1 ip4:203.0.113.10 include:_spf.google.com -all</code></li><li>ESP only (no local MTA): <code>v=spf1 include:sendgrid.net -all</code></li></ul><p>Use <code>-all</code> (hard fail) once you are sure the list is complete. Avoid <code>+all</code>. Prefer <code>~all</code> only during short migration windows. Keep the record under the DNS 255-character string limit; split long include chains carefully or use a subdomain for bulk mail.</p></div><div class=\"onpage-section\"><h2>2. DKIM: Sign Every Message</h2><p>DKIM attaches a cryptographic signature to outbound messages. Receivers verify the signature against a public key published in DNS.</p><ol><li>Generate a 2048-bit RSA key pair on the mail host (OpenDKIM, rspamd, or your MTA’s built-in signer).</li><li>Configure the MTA to sign mail for each sending domain with a stable selector (e.g. <code>mail</code> or <code>s1</code>).</li><li>Publish the public key as TXT at <code>selector._domainkey.example.com</code>.</li><li>Verify with an external checker that signatures pass on a real test message.</li></ol><p>Rotate keys on a schedule (for example every 12 months). Publish the new selector before switching signing, then remove the old DNS record after TTL expiry.</p></div><div class=\"content-strategy-section\"><h2>3. DMARC: Monitor, Then Enforce</h2><p>DMARC tells receivers what to do when SPF or DKIM fail alignment with the visible From domain. Start with reporting only:</p><pre><code>v=DMARC1; p=none; rua=mailto:dmarc@example.com; fo=1</code></pre><p>Review aggregate reports for a few weeks. Legitimate mail must pass SPF or DKIM alignment. Then move to:</p><pre><code>v=DMARC1; p=quarantine; pct=25; rua=mailto:dmarc@example.com</code></pre><p>Increase pct and eventually set <code>p=reject</code> when reports are clean. Without a monitored rua mailbox, you are flying blind.</p></div><div class=\"local-seo-section\"><h2>4. Multi-Domain Alignment</h2><p>Each sending domain needs its own SPF, DKIM, and DMARC records. DMARC alignment requires that the domain in the From header aligns with the domain that passed SPF and/or DKIM. Common failure mode: the envelope sender is on a shared mail subdomain while From is the customer domain and neither SPF nor DKIM aligns.</p><p>Shared IP reputation matters. One compromised form or open relay on the VPS can blacklist every domain. Monitor blocklists (Spamhaus and similar) and keep submission authenticated (SASL) so random processes cannot inject mail.</p></div><div class=\"analytics-section\"><h2>5. PTR, HELO, and TLS</h2><ul><li>Reverse DNS (PTR) for the sending IP should match a hostname you control; forward DNS for that hostname should resolve back to the same IP.</li><li>SMTP HELO/EHLO name should match that hostname.</li><li>Offer TLS for outbound submission and prefer TLS when delivering to remote MX hosts.</li><li>Use consistent From and Reply-To addresses that exist and accept bounces.</li></ul></div><div class=\"performance-section\"><h2>6. Production Checklist</h2><ul><li>Single SPF TXT per domain with <code>-all</code> and only real includes/IPs</li><li>DKIM signing verified end-to-end for each domain</li><li>DMARC rua monitored; policy progressing toward reject</li><li>PTR and HELO aligned</li><li>No open relay; submission requires auth</li><li>Bounce and complaint handling configured</li><li>Test with mail-tester or equivalent before cutting over production traffic</li></ul></div><div class=\"faq-section\"><h2>7. FAQ</h2><div class=\"faq-item\"><h3>Can one SPF record cover all domains on the VPS?</h3><p>No. SPF is published per domain (or subdomain). Each From domain needs its own record.</p></div><div class=\"faq-item\"><h3>Should I send app mail through an ESP instead?</h3><p>Often yes for marketing volume. For low-volume transactional mail, a well-configured VPS is fine if SPF/DKIM/DMARC and IP reputation are maintained.</p></div><div class=\"faq-item\"><h3>Why does Gmail still spam my mail?</h3><p>Check alignment, content, sudden volume spikes, and whether the IP or domain is listed. DMARC pass alone does not guarantee inbox placement.</p></div></div><div class=\"conclusion-section\"><h2>8. Summary</h2><p>SPF authorizes senders, DKIM proves integrity, and DMARC enforces alignment policy. On a multi-domain VPS, configure all three per domain, align PTR/HELO, and only then trust transactional mail to reach the inbox.</p><div class=\"final-takeaway\"><h3>Key Takeaway</h3><p><em>Authorize narrowly with SPF, sign with DKIM, monitor then enforce with DMARC, and keep PTR, HELO, and TLS consistent with the domains you send as.</em></p></div><hr><div class=\"cta-section\"><h3>Email from your VPS landing in spam?</h3><p>I set up SPF, DKIM, DMARC, and mail stacks for multi-domain hosts. <a href=\"/contact\" style=\"color: #39FF14;\">Get in touch</a> or see <a href=\"/services\" style=\"color: #39FF14;\">DevOps services</a>.</p><p>Related reading: <a href=\"/blog/deploying-multi-site-nextjs-vps-nginx\">Deploy Multi-Site Next.js on VPS</a>.</p></div></div>`
export const date = '2026-04-28'
export const readTime = '11 min read'
export const category = 'DevOps'
export const author = 'Mussawar Hayat'
export const keywords = ['SPF', 'DKIM', 'DMARC', 'email deliverability', 'VPS email setup', 'DNS', 'transactional email', 'multi-domain mail', 'OpenDKIM']
