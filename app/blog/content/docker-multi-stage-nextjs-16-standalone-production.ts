export const title = 'Docker Multi-Stage Builds for Next.js 16 Production'
export const excerpt = 'Ship a minimal, secure Next.js 16 App Router image with output: "standalone", multi-stage builds, non-root user, and only the files the runtime needs. Production Dockerfile, .dockerignore, and checklist.'
export const content = `<div class="intro-section"><h2>Why Single-Stage Next.js Docker Images Fail in Production</h2><p class="lead-paragraph">A naive Dockerfile that runs <code>npm install</code> and <code>npm run build</code> in one stage often produces images over 1 GB. They include build tools, the full <code>node_modules</code>, source maps, and cache directories you never need at runtime. Multi-stage builds with Next.js <code>output: "standalone"</code> cut that footprint dramatically while keeping Server Components, Server Actions, and API routes intact.</p><div class="key-highlights"><h3>What You Will Learn</h3><ul><li>How <code>output: "standalone"</code> works and what it traces</li><li>A production multi-stage Dockerfile for Next.js 16 App Router</li><li>Correct copy of <code>.next/standalone</code>, <code>.next/static</code>, and <code>public</code></li><li>Non-root user, hostname, and security defaults</li><li><code>.dockerignore</code> rules that keep the build context small</li><li>Common mistakes that break CSS, images, or server startup</li><li>A short production checklist</li></ul></div></div><div class="technical-section"><h2>1. Enable Standalone Output</h2><p>In <code>next.config.ts</code> (or <code>next.config.js</code>) set:</p><pre><code>import type { NextConfig } from 'next'\n\nconst nextConfig: NextConfig = {\n  output: 'standalone',\n}\n\nexport default nextConfig\n</code></pre><p>When you run <code>next build</code>, Next.js traces the files required to run the server and writes a minimal tree under <code>.next/standalone</code>. That tree includes a generated <code>server.js</code> and only the dependencies the app actually uses. Static assets and the public folder are not fully inlined into standalone, so you must copy them explicitly in the final image stage.</p><p>Standalone supports the full App Router feature set: Server Components, Server Actions, Route Handlers, middleware, and ISR. Use static export only when you intentionally drop server features.</p></div><div class="onpage-section"><h2>2. Production Multi-Stage Dockerfile</h2><p>Three stages keep install, build, and runtime separate. The final stage never sees your source or full <code>node_modules</code>.</p><pre><code># syntax=docker/dockerfile:1\n\nARG NODE_VERSION=22-alpine\n\n# --- deps ---\nFROM node:${NODE_VERSION} AS deps\nWORKDIR /app\nCOPY package.json package-lock.json* yarn.lock* pnpm-lock.yaml* ./\nRUN if [ -f package-lock.json ]; then npm ci; \\\n    elif [ -f yarn.lock ]; then corepack enable yarn && yarn install --frozen-lockfile; \\\n    elif [ -f pnpm-lock.yaml ]; then corepack enable pnpm && pnpm install --frozen-lockfile; \\\n    else echo "Lockfile required" && exit 1; fi\n\n# --- builder ---\nFROM node:${NODE_VERSION} AS builder\nWORKDIR /app\nCOPY --from=deps /app/node_modules ./node_modules\nCOPY . .\nENV NEXT_TELEMETRY_DISABLED=1\nENV NODE_ENV=production\nRUN if [ -f package-lock.json ]; then npm run build; \\\n    elif [ -f yarn.lock ]; then yarn build; \\\n    elif [ -f pnpm-lock.yaml ]; then pnpm build; \\\n    else exit 1; fi\n\n# --- runner ---\nFROM node:${NODE_VERSION} AS runner\nWORKDIR /app\nENV NODE_ENV=production\nENV NEXT_TELEMETRY_DISABLED=1\nENV PORT=3000\nENV HOSTNAME=0.0.0.0\n\nRUN addgroup --system --gid 1001 nodejs \\\n  && adduser --system --uid 1001 nextjs\n\nCOPY --from=builder /app/public ./public\nCOPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./\nCOPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static\n\nUSER nextjs\nEXPOSE 3000\nCMD ["node", "server.js"]\n</code></pre><p>Key points:</p><ul><li><code>HOSTNAME=0.0.0.0</code> so the process listens on all interfaces inside the container (required for published ports).</li><li>Copy order: <code>public</code>, then standalone root, then <code>.next/static</code>. Missing the static copy is the most common cause of missing CSS and client bundles.</li><li>Run as non-root <code>nextjs</code> user.</li><li>Pin a specific Node major (or digest) in CI rather than floating <code>latest</code>.</li></ul></div><div class="content-strategy-section"><h2>3. .dockerignore</h2><p>Keep the build context small and avoid shipping secrets or local artifacts:</p><pre><code>Dockerfile\n.dockerignore\nnode_modules\n.next\n.git\n.gitignore\nREADME.md\n.env*\n!.env.example\nnpm-debug.log*\nyarn-debug.log*\nyarn-error.log*\ncoverage\n.nyc_output\n.vscode\n.idea\n*.md\n</code></pre><p>Do not copy <code>.env</code> files into the image. Pass runtime secrets with environment variables or your orchestrator’s secret store.</p></div><div class="local-seo-section"><h2>4. Build and Run</h2><pre><code>docker build -t nextjs-app:prod .\ndocker run --rm -p 3000:3000 \\\n  -e DATABASE_URL="..." \\\n  -e AUTH_SECRET="..." \\\n  nextjs-app:prod\n</code></pre><p>For Compose, map the same port and inject env vars. Prefer explicit <code>env_file</code> or secrets over baking values into the image.</p><p>Image size after a correct multi-stage standalone build is typically a few hundred megabytes instead of a full gigabyte-plus single-stage image, depending on your dependency graph and public assets.</p></div><div class="linkbuilding-section"><h2>5. Security and Operational Defaults</h2><ul><li><strong>Non-root user</strong> — the sample creates a dedicated <code>nextjs</code> user and switches to it before <code>CMD</code>.</li><li><strong>Read-only root filesystem</strong> — where your platform allows, run the container with a read-only root and a writable volume only for paths that must change (for example temporary upload dirs if you use them).</li><li><strong>No build tools in runtime</strong> — the runner stage never installs compilers or the full package manager tree.</li><li><strong>Healthchecks</strong> — add a simple HTTP check against a lightweight route if your orchestrator supports it.</li><li><strong>Logs</strong> — write to stdout/stderr so Docker and your log driver capture application output without file volumes.</li></ul></div><div class="analytics-section"><h2>6. Common Mistakes</h2><ul><li><strong>Forgetting <code>.next/static</code></strong> — pages load but styles and client JS 404.</li><li><strong>Forgetting <code>public</code></strong> — images and static files under <code>/public</code> disappear.</li><li><strong>Listening on localhost only</strong> — without <code>HOSTNAME=0.0.0.0</code> the process is unreachable from outside the container.</li><li><strong>Copying the whole <code>node_modules</code> into the runner</strong> — defeats standalone tracing and bloats the image.</li><li><strong>Building without a lockfile</strong> — non-reproducible installs and surprise dependency versions in CI.</li><li><strong>Running as root</strong> — unnecessary privilege if the process is compromised.</li></ul></div><div class="conclusion-section"><h2>7. Production Checklist</h2><ul><li><code>output: 'standalone'</code> in Next config</li><li>Multi-stage Dockerfile: deps → builder → runner</li><li>Copy <code>public</code>, <code>.next/standalone</code>, and <code>.next/static</code></li><li><code>HOSTNAME=0.0.0.0</code> and non-root user</li><li>Strict <code>.dockerignore</code></li><li>Secrets only via environment or secret manager</li><li>Image scanned in CI; Node base image pinned</li><li>Smoke test: homepage, a Server Action, and a static asset after deploy</li></ul><h2>Summary</h2><p>For Next.js 16 App Router on Docker, standalone output plus a three-stage build is the standard production pattern. You keep full server capabilities while shipping only the traced runtime, static chunks, and public assets. The same approach works on a single VPS, Kubernetes, or any container platform.</p><div class="final-takeaway"><h3>Key Takeaway</h3><p><em>Standalone traces what the server needs. Your Dockerfile’s only job is to copy that tree, static assets, and public files into a minimal non-root image that listens on 0.0.0.0.</em></p></div><hr><div class="cta-section"><h3>Need a hardened Next.js deploy pipeline?</h3><p>I set up multi-stage Docker builds, VPS Nginx reverse proxies, and CI for production Next.js apps. <a href="/contact" style="color: #39FF14;">Get in touch</a> if you want the same stack applied to your project.</p></div></div>`
export const date = '2026-08-02'
export const readTime = '11 min read'
export const category = 'DevOps'
export const author = 'Mussawar Hayat'
export const keywords = ['Docker', 'Next.js 16', 'multi-stage build', 'standalone', 'production', 'App Router', 'container', 'DevOps']
