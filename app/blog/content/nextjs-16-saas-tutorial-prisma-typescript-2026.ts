export const title = 'Next.js 16 + Prisma SaaS Tutorial'
export const excerpt = 'Complete step-by-step guide to building a scalable SaaS using Next.js 16 App Router, Prisma Postgres, Auth.js, and Tailwind CSS. Production patterns, Server Actions, and deployment.'
export const content = `<div class="intro-section"><h2>Build a Production SaaS with Next.js 16</h2><p class="lead-paragraph">This tutorial walks through a scalable SaaS skeleton on Next.js 16 App Router with Prisma, Auth.js, PostgreSQL, and Tailwind CSS. Focus is on production patterns: Server Components for data, thin Server Actions, a server-only DAL, and deploy-ready configuration.</p><div class="key-highlights"><h3>What You Will Build</h3><ul><li>App Router project structure for multi-tenant SaaS</li><li>Prisma schema and singleton client</li><li>Auth.js session gate and protected routes</li><li>Server Actions for create/update with validation</li><li>Deployment notes for Vercel and VPS</li></ul></div></div><div class="technical-section"><h2>1. Project Shape</h2><pre><code>app/\n  (marketing)/page.tsx\n  (app)/dashboard/page.tsx\n  actions/\nlib/\n  prisma.ts\n  auth.ts\ndata/   # server-only DAL\nprisma/schema.prisma\n</code></pre></div><div class="onpage-section"><h2>2. Prisma Schema Baseline</h2><p>Use a User model with related Project rows owned by user id. Apply the singleton client pattern and set connection_limit for your host.</p></div><div class="content-strategy-section"><h2>3. Auth.js Gate</h2><p>Protect dashboard layouts by reading the session on the server and redirecting unauthenticated users.</p><pre><code>// app/(app)/layout.tsx\nimport { auth } from '@/lib/auth'\nimport { redirect } from 'next/navigation'\n\nexport default async function AppLayout({ children }: { children: React.ReactNode }) {\n  const session = await auth()\n  if (!session?.user) redirect('/login')\n  return &lt;div className="app-shell"&gt;{children}&lt;/div&gt;\n}\n</code></pre></div><div class="local-seo-section"><h2>4. Thin Actions + DAL</h2><p>Keep <code>use server</code> files thin. Put Zod validation, session checks, and Prisma calls in <code>import 'server-only'</code> modules. Return DTOs only.</p></div><div class="analytics-section"><h2>5. Deploy</h2><ul><li>Vercel: DATABASE_URL (Accelerate recommended), AUTH_SECRET, OAuth credentials</li><li>VPS: PM2 + Nginx, migrations in CI, pooling in front of Postgres</li></ul></div><div class="conclusion-section"><h2>6. Production Checklist</h2><ul><li>Singleton Prisma client</li><li>Auth on layouts and every mutation</li><li>Zod on all Server Action inputs</li><li>Ownership checks on update/delete</li><li>Minimal use client boundaries</li></ul><h2>Summary</h2><p>A durable SaaS on Next.js 16 is about boundaries: server by default, validated actions, and a clear DAL.</p><div class="final-takeaway"><h3>Key Takeaway</h3><p><em>Structure for multi-tenant safety from day one: session-gated layouts, thin actions, and ownership-scoped Prisma queries.</em></p></div><hr><div class="cta-section"><h3>Building a SaaS and need an architecture review?</h3><p>I help teams set up Next.js 16 + Prisma foundations that scale. <a href="/contact" style="color: #39FF14;">Get in touch</a>.</p></div></div>`
export const date = '2026-07-26'
export const readTime = '14 min read'
export const category = 'Full-Stack'
export const author = 'Mussawar Hayat'
export const keywords = ['Next.js 16', 'SaaS', 'Prisma', 'Auth.js', 'TypeScript', 'App Router', 'PostgreSQL']
