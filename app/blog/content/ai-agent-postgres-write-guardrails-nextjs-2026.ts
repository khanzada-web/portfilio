export const title = 'Let AI Agents Write to Postgres in Next.js Without Wrecking Production (2026 Guardrails)'
export const excerpt = 'Agents now write data, not just read it. This production guide shows how to give a TypeScript / Next.js agent scoped Postgres write access with Zod contracts, idempotency keys, approval gates, Prisma transactions, audit logs, and blast-radius limits.'
export const content = `<div class=\"intro-section\"><h2>Agents write data now. Your database is the blast radius.</h2><p class=\"lead-paragraph\">In 2026 most teams that run agents no longer stop at read-only tools. Support bots create tickets. Coding agents insert fixtures. Ops agents flip feature flags. The useful part is also the dangerous part: a model with a write path can persist a bad plan as durable state.</p><p>This guide is the production pattern for Next.js and Node.js teams that want an agent to mutate Postgres through Prisma without handing it a superuser connection string. You will build a write surface that looks like an internal API: typed inputs, tenant-scoped queries, idempotent mutations, optional human approval, and an audit trail the model cannot edit.</p><div class=\"key-highlights\"><h3>What you will implement</h3><ul><li>A dedicated agent write role in Postgres (no DDL, no cross-tenant tables)</li><li>Zod-validated write tools with explicit allowlists</li><li>Idempotency keys so retries do not double-insert</li><li>Approval gates for destructive or high-value writes</li><li>Prisma interactive transactions with a sensible isolation level</li><li>Append-only audit logs and rate limits per principal</li></ul></div></div><div class=\"technical-section\"><h2>1. The problem</h2><p>A coding or product agent that can call <code>prisma.order.create</code> directly is one hallucinated foreign key away from corrupting billing. Common failure modes:</p><ul><li>The model invents an <code>ownerId</code> and writes into another tenant.</li><li>A retry after a timeout inserts the same row twice.</li><li>A cleanup tool deletes more rows than the prompt intended.</li><li>The agent dumps connection strings or raw rows back into the chat.</li><li>Write tools share the same DB user as migrations and admin jobs.</li></ul><p>None of these are model problems. They are API-design problems. Treat the agent like an untrusted client that already passed authentication at the edge.</p></div><div class=\"onpage-section\"><h2>2. Architecture</h2><p>Keep three layers separate:</p><ol><li><strong>Host / agent runtime</strong> — Claude Code, Cursor, or your own orchestrator. It authenticates the user and may only send a verified <code>actorId</code> plus a tool name and arguments.</li><li><strong>Write gateway</strong> — a Next.js Route Handler or a small Node service. It validates schemas, enforces policy, and talks to Prisma.</li><li><strong>Database</strong> — Postgres with a least-privilege role, row-level checks in the DAL, and an append-only audit table.</li></ol><p>The model never receives <code>DATABASE_URL</code>. It never chooses a tenant id. It never runs raw SQL.</p></div><div class=\"content-strategy-section\"><h2>3. Postgres role and schema</h2><p>Create a role that can DML only the tables the agent is allowed to touch. Run this as a migration owner, not as the agent:</p><pre><code>CREATE ROLE agent_writer LOGIN PASSWORD 'rotate-me';\nGRANT CONNECT ON DATABASE app TO agent_writer;\nGRANT USAGE ON SCHEMA public TO agent_writer;\nGRANT SELECT, INSERT, UPDATE ON TABLE \